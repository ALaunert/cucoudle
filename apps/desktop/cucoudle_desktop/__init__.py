"""Cucoudle desktop daemon package.

Managed PTY bridge and shell shims that let a phone remotely observe and steer
local CLI agent sessions (claude, codex, cursor, ...) through a relay.
"""

APP_VERSION = "0.1.2"
